# 服务端口（与官方 backend-mock 一致，前端 vite 代理零改动）
PORT=5320

# JWT 密钥（生产环境务必替换为强随机串，两把密钥不要相同）
ACCESS_TOKEN_SECRET=change-me-access-token-secret
REFRESH_TOKEN_SECRET=change-me-refresh-token-secret

# MySQL 连接串（Drizzle ORM）
DATABASE_URL=mysql://root:123456@localhost:3306/vben_admin
